(function(window,undefined){
	//cache jQuery
	var $ = window.jQuery;
	//init carousel
	$(".fn-carousel").dtCarousel({autoPlay:true,interval:3000,fadeSpeed:500});
	//some other bindings eg. sticky footer
})(window);
	
